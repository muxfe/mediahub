extends ../layout
block content
  div(class="page")
    div(class="page-header")
      h1(class="page-title") 所有文件列表
    div(class="page-body")
      div(class="weui-cells")
        a(href='/file' + basePath, class="weui-cell weui-cell_access")
          div(class="weui-cell__bd")
            p
              i(class="fa fa-folder-open") #{' .'}
        a(href=prevPath, class="weui-cell weui-cell_access")
          div(class="weui-cell__bd")
            p
              i(class="fa fa-folder-open") #{' ..'}
        each val, index in list
          - if (val.type === 'dir')
            a(href='/file' + basePath + '/' + index, class="weui-cell weui-cell_access")
              div(class="weui-cell__bd")
                p
                  i(class="fa fa-folder-open") #{' ' + val.filename}
        each val, index in list
          - if (val.type !== 'dir')
            a(href='/file' + basePath + '/' + index, class="weui-cell weui-cell_access")
              div(class="weui-cell__bd")
                p
                  i(class="fa fa-file-o") #{' ' + val.filename}

          div(class="weui-cell__ft")
